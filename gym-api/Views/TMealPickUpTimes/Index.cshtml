@model IEnumerable<gym_api.Models.TMealPickUpTime>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FStartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FEndTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FIsActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FStartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FEndTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FIsActive)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FPickUpTimeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FPickUpTimeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FPickUpTimeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
